<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Craft Starter</title>
    <link rel="stylesheet" href="{{ rev('/assets/main.css') }}">
    <script src="{{ rev('/assets/main.js') }}" defer></script>
  </head>
  <body class="{{ craft.app.request.getSegment(1) ?: 'home' }} flex flex-col min-h-screen font-body">
    <main class="flex-grow">
      {% block content %}
      {% endblock %}
    </main>
  </body>
</html>
